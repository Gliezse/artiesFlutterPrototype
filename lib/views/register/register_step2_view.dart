import 'package:flutter/material.dart';

class RegisterStep2View extends StatelessWidget {
  
  static final String route = "/register.step2";
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text("funca nashe"),
        ),
      ),
    );
  }
}